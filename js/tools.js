import{head,modal,toast,$,esc}from'./ui.js';
const tools=[['qr','QR Oluşturucu'],['password','Şifre üretici'],['counter','Metin sayacı'],['base64','Base64'],['url','URL kodlama'],['json','JSON biçimlendirici'],['timestamp','Timestamp'],['random','Rastgele sayı'],['uuid','UUID'],['size','Dosya boyutu']];
function openTool(type,title){let body=`<div class="field"><label>Girdi</label><textarea id="tool-input"></textarea></div><div class="actions"><button class="primary" id="tool-run">Çalıştır</button></div><div class="field"><label>Sonuç</label><textarea id="tool-output" readonly></textarea></div>`;if(type==='password')body=`<div class="field"><label>Uzunluk</label><input id="tool-input" type="number" value="20" min="8" max="100"></div><div class="actions"><button class="primary" id="tool-run">Üret</button></div><input id="tool-output" readonly>`;if(type==='random')body=`<div class="form-grid"><input id="min" type="number" value="1"><input id="max" type="number" value="100"></div><div class="actions"><button class="primary" id="tool-run">Üret</button></div><input id="tool-output" readonly>`;if(type==='qr')body=`<div class="field"><label>Metin veya bağlantı</label><input id="tool-input"></div><div class="actions"><button class="primary" id="tool-run">QR oluştur</button></div><div id="tool-output" style="display:grid;place-items:center;margin-top:15px"></div>`;modal(title,body);$('#tool-run').onclick=()=>{try{const i=$('#tool-input')?.value||'',out=$('#tool-output');let v='';if(type==='counter')v=`${i.length} karakter · ${i.trim()?i.trim().split(/\s+/).length:0} kelime`;if(type==='base64')v=btoa(unescape(encodeURIComponent(i)));if(type==='url')v=encodeURIComponent(i);if(type==='json')v=JSON.stringify(JSON.parse(i),null,2);if(type==='timestamp')v=new Date(/^\d+$/.test(i)?Number(i):i).toISOString();if(type==='uuid')v=crypto.randomUUID();if(type==='size')v=`${(+i/1024/1024).toFixed(2)} MB · ${(+i/1024/1024/1024).toFixed(3)} GB`;if(type==='password'){const chars='ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz23456789!@#$%';v=Array.from(crypto.getRandomValues(new Uint32Array(+i)),x=>chars[x%chars.length]).join('')}if(type==='random')v=Math.floor(Math.random()*(+$('#max').value-+$('#min').value+1)) + +$('#min').value;if(type==='qr'){const url='https://api.qrserver.com/v1/create-qr-code/?size=220x220&data='+encodeURIComponent(i);out.innerHTML=`<img width="220" height="220" alt="QR kod" src="${url}">`;return}out.value=v;toast('Araç çalıştırıldı')}catch(e){toast('Girdi geçerli değil')}}}
export function renderTools(root){const info=[['Tarayıcı',navigator.userAgent.match(/(Firefox|Edg|Chrome|Safari)\/?[\d.]*/)?.[0]||'Bilinmiyor'],['Ekran',`${screen.width} × ${screen.height}`],['Viewport',`${innerWidth} × ${innerHeight}`],['CPU',navigator.hardwareConcurrency?navigator.hardwareConcurrency+' iş parçacığı':'Desteklenmiyor'],['Dil',navigator.language],['Saat dilimi',Intl.DateTimeFormat().resolvedOptions().timeZone],['Bağlantı',navigator.onLine?'Çevrimiçi':'Çevrimdışı']];root.innerHTML=head('PC Tools','Günlük işler için hızlı, tarayıcı tabanlı araçlar')+`<div class="tool-grid">${tools.map(([id,n])=>`<div class="card tool-card" data-tool="${id}"><span class="badge">⌁</span><b>${n}</b><span class="muted">Aç ve kullan</span></div>`).join('')}</div><div class="card" style="margin-top:16px"><h2>Sistem bilgileri</h2><div class="grid three-col">${info.map(([k,v])=>`<div><span class="muted">${k}</span><div>${esc(v)}</div></div>`).join('')}</div></div>`;root.querySelectorAll('[data-tool]').forEach((b,i)=>b.onclick=()=>openTool(b.dataset.tool,tools[i][1]))}
