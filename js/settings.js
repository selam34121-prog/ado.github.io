import{settings,exportData,importData}from'./storage.js';import{head,toast,$}from'./ui.js';
export function applySettings(){document.documentElement.dataset.accent=settings.get('accent','purple');document.documentElement.dataset.light=settings.get('light',false)}
export function renderSettings(root){const accent=settings.get('accent','purple'),colors={purple:'#7c5cff',blue:'#4c8dff',cyan:'#16c7d9',red:'#ff5268',green:'#35c987',orange:'#ff914d',pink:'#f260d1'};root.innerHTML=head('Ayarlar','Görünüm, yapay zekâ ve veri yönetimi')+`<div class="grid two-col"><div class="card"><h2>Görünüm</h2><p class="muted">Vurgu rengi</p><div class="swatches">${Object.entries(colors).map(([k,v])=>`<button class="swatch ${accent===k?'active':''}" data-accent="${k}" style="background:${v}" aria-label="${k}"></button>`).join('')}</div><div class="list-item" style="margin-top:18px"><div><b>Açık tema</b><div class="muted">Arayüz renk düzeni</div></div><button class="secondary" id="theme-toggle">Değiştir</button></div></div><div class="card"><h2>Gemini proxy</h2><div class="field"><label>Cloudflare Worker URL</label><input id="ai-endpoint" value="${settings.get('aiEndpoint','https://ado-gemini-proxy.selam34121.workers.dev')}"></div><div class="actions"><button class="primary" id="save-ai">Kaydet</button></div><p class="muted">API anahtarı burada tutulmaz.</p></div><div class="card"><h2>Yedekleme</h2><p class="muted">Tüm yerel verileri tek JSON dosyasında dışa veya içe aktar.</p><div class="actions"><label class="secondary">İçe aktar<input id="import-file" type="file" accept="application/json" hidden></label><button class="primary" id="export-data">Dışa aktar</button></div></div></div>`;root.querySelectorAll('[data-accent]').forEach(b=>b.onclick=()=>{settings.set('accent',b.dataset.accent);applySettings();renderSettings(root)});$('#theme-toggle').onclick=()=>{settings.set('light',!settings.get('light',false));applySettings()};$('#save-ai').onclick=()=>{settings.set('aiEndpoint',$('#ai-endpoint').value.trim());toast('AI ayarı kaydedildi')};$('#export-data').onclick=async()=>{const blob=new Blob([JSON.stringify(await exportData(),null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`ado-dashboard-backup-${new Date().toISOString().slice(0,10)}.json`;a.click();URL.revokeObjectURL(a.href);toast('Yedek dışa aktarıldı')};$('#import-file').onchange=async e=>{if(!confirm('Mevcut veriler değiştirilecek. Yedeği içe aktarmak istiyor musun?'))return;try{await importData(JSON.parse(await e.target.files[0].text()));applySettings();toast('Yedek içe aktarıldı')}catch{toast('Yedek dosyası geçersiz')}}}
