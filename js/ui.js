export const $=(s,p=document)=>p.querySelector(s),$$=(s,p=document)=>[...p.querySelectorAll(s)];export const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));export const money=(n,c='TRY')=>new Intl.NumberFormat('tr-TR',{style:'currency',currency:c}).format(Number(n)||0);export const date=d=>new Intl.DateTimeFormat('tr-TR',{dateStyle:'medium'}).format(new Date(d));
export function toast(text){const el=document.createElement('div');el.className='toast';el.textContent=text;$('#toast-root').append(el);setTimeout(()=>{el.style.opacity=0;el.style.transform='translateX(15px)';setTimeout(()=>el.remove(),200)},2600)}
export function modal(title,body){$('#modal-root').innerHTML=`<div class="modal-wrap"><div class="modal"><div class="modal-head"><h2>${esc(title)}</h2><button class="icon-btn" data-close>×</button></div>${body}</div></div>`;const close=()=>$('#modal-root').replaceChildren();$('[data-close]').onclick=close;$('.modal-wrap').onclick=e=>{if(e.target.classList.contains('modal-wrap'))close()};return close}
export const head=(title,desc,action='')=>`<div class="page-head"><div><h1>${title}</h1><p>${desc}</p></div>${action}</div>`;
export const empty=(title,desc,action,label)=>`<div class="empty"><b>${title}</b><span>${desc}</span>${action?`<div style="margin-top:16px"><button class="primary" data-action="${action}">${label}</button></div>`:''}</div>`;
